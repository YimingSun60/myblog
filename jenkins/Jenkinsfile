pipeline {
    agent any

    tools{
        go '1.21'
    }

    stages {
        stage('Build') {
            steps {
                sh 'go build -o blog .'
            }
        }
        stage('Deploy') {
            sshagent(['ec2']) {
                sh 'ls'
            // some block
            }
        }
    }
}